# 训练暂停：先修复环境与补齐数据

用户于2026-09-11要求停止当前训练，先修复环境、补齐数据，再重新训练。
本记录是工作交接约束，不是代码级启动锁；不要因旧进度未达到50步而自动续跑。

## 已执行与核验

- 停止服务：`sitian-training-chain-20260911-r3.service`。
- 运行标识：`qwen3_8b_training_chain_20260911_r3`。
- 2026-09-11 23:39（Asia/Shanghai）核验：服务inactive/dead、MainPID=0，GPU计算进程查询为空，未发现训练链、PPO或vLLM训练工作进程残留。
- 服务为临时单元，配置Restart=no，Triggers与TriggeredBy均为空；未发现该服务自动续跑入口。
- 原日志、训练进度、checkpoint和冻结数据均保留，没有删除或覆盖。
- progress.json报告7/50次有效更新；latest_checkpointed_iteration.txt为7，global_step_7/actor下四个rank的model、optim和extra_state文件均存在。这里只核验保存标记及文件存在，未做加载恢复测试。未保存的在途计算不视为保留。
- 该运行是用户主动中止，不能记为完整50步训练完成，也不能当作完整实验结论。

## 重新训练前的验收条件

1. 环境与harness：明确本轮能力范围，修复确定的问题；验证工具可用性、时间门禁、缺失数据行为和训练/推理接口一致性。相似案例检索等讨论中的能力不得未经实现验收就算作已有。
2. 原始资料：在9800X3D本机核查外置盘，按交接清单定位752个未构建case的原因；复核491个隔离case，仅恢复真正满足有效性条件的案例，不降低真值要求。
3. 数据口径：处理或明确记录CAMS D5缺口、O3指导与真值口径差异、冬季覆盖以及事件窗口重叠问题，不以文件数量齐全替代质量验收。
4. 新版本：在独立目录补建，重新审计来源、发布时间、目标日隔离和训练/验证/测试边界；重建偏差索引、Parquet及输入身份，冻结新实验协议。现有修复快照尚不等于训练就绪。
5. 启动：上述验收完成后再安排新训练；旧checkpoint仅保留作追溯，不能默认在更换数据/环境后续接并沿用旧实验身份。

原始数据检查与重建细节见 `docs/LOCAL_DATA_REBUILD_HANDOFF_2026-09-11.md`。
