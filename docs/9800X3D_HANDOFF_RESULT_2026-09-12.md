# 9800X3D 原盘处理回传结果（2026-09-12交接）

执行日期：2026-09-11（9800X3D本机日期）。源项目 `/root/sitian`；外接盘 `/mnt/wsl/EAGET`（`/mnt/eaget` 指向该挂载点）。

已同步训练机与GitHub共同的 `c56fe10912c18b279e524bd6e9deaba41caf1a0d`，保留此前本机审计提交，合并到 `codex/raw-data-handoff-20260912`。同步前411个旧代码/参考文件已移至本机 `data/local_handoff_20260912/pre_sync_code` 并核对SHA。原始资料、旧case、split与checkpoint保留。

本轮按HANDOFF先提取3个样例，再提取8,300个完整请求。所有输出保存在本机 `data/local_handoff_20260912/`。**只回传城市时序、必要已有证据/文本和审计清单，不传原始NetCDF/GRIB、完整城市观测档案或模型权重；不另打任务压缩包。**

## 回传目录

| 子目录 | 内容 | 数据规模 |
|---|---|---|
| `batch1` | 北京2026-05-11、北京2025-12-18、九江2025-04-25原生样例 | 30条CAMS变量时序；4,473条观测记录；0提取失败 |
| `batch2` | 完整请求的原生CAMS与城市小时观测 | 8,300案例；83,260条CAMS时序；84,644条观测记录；0提取失败 |
| `evidence` | 现有城市形势/成分/火点/空间证据与原始来源sidecar | 1,604个issue/channel记录及静态上下文；301,920个GFS/IFS城市时次；16,040条原始GRIB来源记录 |
| `regional` | 按请求城市筛选的CMAQ/NAQP/WRF站点原行 | 7,839,827行；1,854个实际使用源CSV的SHA；缺失项另列 |
| `expert` | 真实会商来源文本及已有蒸馏文件 | 46个源文件；294个导出记录，其中整份会商JSON包含多个原文片段 |
| `quality` | 原生提取校验、8,300例质量标记、752未建/491隔离复核 | 清单不替代训练准入 |
| `provenance` | 精确执行脚本字节快照、测试日志与同步记录 | 用于复算源脚本SHA；Git最终版本另含导入方式修复 |

合计约355 MB（338 MiB），以最终 `DELIVERY_MANIFEST.json` 的逐文件字节数为准。各 `.jsonl.gz` 已压缩；传输按目录文件进行。

## 原生CAMS与观测

- 每个请求都有pm2p5、pm10、u10、v10、t2m、d2m、blh、tp、tcc、go3十类变量。
- 保留实际reference time、lead/valid time、原单位、原生层次、缺测掩膜和选中格点；没有聚合、插值、填零或自行转换近地浓度。
- O3保留模式层137（原坐标单位`1`）与质量混合比单位，未将3小时瞬时最大当成八小时日最大。
- 所有O3时序D1—D5的原生样本数为8/8/8/7/0；绝大多数地面变量为4/4/4/4/0，少数较密来源为8/8/8/7/0。D4时间轴不能按完整24小时序列理解，D5没有原生指导。
- 2025-12-19的26个请求在sfc/o3各遇到分块文件与单日试点文件。显式保留两份来源，所以总计83,260条而非83,000条。52个case-kind重叠组、260个case-variable对在共同格点/单位/时间步上数值全部一致；仍保留原生时间步差异，不自动拼接或去重。
- 83,260条记录的形状、时间、掩膜、层次结构检查全部通过；另随机抽取60条逐值回查原NetCDF。611个实际使用的CAMS/城市CSV源文件已重新计算SHA，未变化。
- 观测导出覆盖所有请求所需前3日至后5日，保留六污染物和O3_8h原始字符串。没有把`0`当缺测，也没有把异常字符串洗成有效真值。

## 质量与752个未建案例

`quality/unbuilt_case_findings.json` 按本次实际提取/原始小时数据给出752例的逐例原因：

- 649例在基础case层没有剩余阻塞；
- 76例五天真值的有效小时数不足；
- 27例输入窗口不足48个时间戳；
- 752例的CAMS当前全部可取得，不沿用旧日志把725例继续标成缺CAMS。

完整8,300例中7,733例通过原始六污染物真值有效性核对，567例不通过（原491例＋新识别76例）。原491个隔离案例全部仍不合格。上述真值计数不包含输入、证据、发布时间和最终切分资格，不能直接作为训练样本数。

同时保留严格72小时输入窗口和旧构建器80小时时间窗的计数，不能把两者混为同一窗口；此次不改门槛、不插值、不把不合格case放回训练。

## GFS/IFS、区域模式及发布时间

`evidence/existing_city_evidence.jsonl.gz` 是**已有派生城市值**，并非本轮重新从GRIB生成的未取整原生时序。原派生值及其层次/时间原样保留，标明`values_are_native=false`。温度和相对湿度只有925/850/700 hPa层；风含925/850/700/500/200 hPa；垂直运动含850/700/500 hPa。稀疏气压层不能解析连续温度廓线或浅薄近地逆温，地形以下层次仍需处理。边界层高度可从本轮CAMS原生blh读取。

原始GRIB来源记录的SHA、文件大小和sidecar均保留。16,040条记录的现存文件大小及sidecar中的SHA与派生引用一致。本轮未重新读取数百GB GRIB逐文件复算其SHA，不能把这项元数据一致性检查表述成全量原始GRIB内容重验。

33个缺失起报日的四类派生证据仍不存在，清单含132个issue/channel缺项并明确列出城市。现有开放证据manifest只覆盖401天，完整请求有434天；本轮没有编造这33天的证据。先前本机标准NWP缓存盘点见原审计，缺少的日期需要另外恢复或下载后再派生。

区域模式保留day/hour、d02/d03、站点和城市原行及原字段，**没有擅自求城市平均或转换单位**。CMAQ/NAQP的日/小时d02/d03各只找到228个所需起报日，另206日起报缺文件；WRF d03对应228日，WRF d02在检查路径未发现。源README随包保留；逐城市无匹配行和缺文件见`regional/manifest.json`，不把它们写成0。

所有CAMS与区域模式的核验发布时间均为null。GFS/IFS旧payload的`available_at`来自固定延迟规则，wrapper和manifest均明确其不是实际发布时间证据。原source sidecar另保留，接收端须区分模型循环、采集时间和真实发布记录，不能因时间门禁按假定延迟通过就认定发布事实已核实。

## JJJ_ATMO

保留22份带original_text与整理文本的会商JSON、24份已有SFT蒸馏文件；原文/清洗合并文本/蒸馏层分开标识。记录原文件SHA、原zip来源SHA、会议日期的文件名依据与文档已有元数据，原zip不传。

`_verified`只视为语料生产者的标签，不代表本轮独立核验。没有可靠原发布时刻的记录一律保留`published_at=null`；这些文本尚不是已验收方法卡，未加入预报case、提示或真值。

## 代码与验收

代码通过Git提交并推送到 `codex/raw-data-handoff-20260912`，实际最终提交见顶层交付manifest。代码改动：原生导出显式保留重叠来源；补充城市证据、区域原行、专家文本提取；新增原生时序校验与逐案例质量复核。10项相关回归测试通过。原生CLI与包导入均检查通过。

执行时的精确脚本字节保存在 `provenance/executed_scripts/`，可与子manifest的extractor SHA核对。Git最终脚本另修复了直接CLI执行与包导入的兼容方式；数据提取逻辑未变。原生样例和全量使用的不同版本分别保存，避免只留最新脚本而丢失实际执行身份。

接收后在本目录运行 `sha256sum -c SHA256SUMS` 核对交付字节。接收团队按照新协议统一重建case、历史/偏差索引与Parquet，核验发布时间和切分隔离后再决定训练准入。本轮没有替换远程main工作区、切换数据入口、启动训练或使用旧checkpoint续跑；`training_ready=false`。
