# Copy to configs/local.env; sourced by Bash launchers. Preserve caller overrides.
# Set only when consuming Parquet copied from the corresponding old project.
# export SITIAN_LEGACY_PROJECT_ROOT="${SITIAN_LEGACY_PROJECT_ROOT:-/root/sitian}"
# export FH_TRAINABLE_MODEL="${FH_TRAINABLE_MODEL:-${project_root}/models/Qwen3-8B}"
# export SITIAN_VERL_ROOT="${SITIAN_VERL_ROOT:-${project_root}/.local/verl-upstream}"
# export CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-0}"

# Harness v2 optional resources. Use identical values when generating tool YAML
# and in every rollout worker; regenerated YAML verifies resource fingerprints.
# export FH_HISTORICAL_CASE_INDEX="/absolute/path/to/historical_cases.json"
# export FH_FORECAST_METHODS="/absolute/path/to/reviewed_methods.json"
# export FH_METHOD_RETRIEVAL=0  # disable method retrieval for a controlled ablation
