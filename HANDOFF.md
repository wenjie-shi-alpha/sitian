# 9800X3D 接手：原盘数据核查与重建

同步本分支后直接在本机修改代码、处理已挂载的原盘。**原始大文件不用传到训练机。**
训练继续暂停；原始文件只读，新结果写入新目录，旧 case、split 和 checkpoint 保留。

## 先做这件事

找到 CAMS 和城市小时观测的实际目录，运行仓库里的提取脚本：

```bash
python3 scripts/export_native_city_data.py \
  --request configs/data_handoff_20260912/request_batch1.json \
  --cams-root /实际路径/CAMS \
  --obs-root /实际路径/城市小时观测 \
  --out /新的输出目录/batch1
```

使用 Python 3.11+，CAMS 提取需要 numpy、netCDF4。首批是北京2026-05-11、
北京2025-12-18、九江2025-04-25；最后一个曾未构建，需核实实际原因。
脚本支持 `sfc_起始日期_结束日期.nc`、`o3_起始日期_结束日期.nc` 和
`china_cities_YYYYMMDD.csv`。格式不同时，在本机根据真实元数据适配代码，保留修改。

核对输出中的时间轴、单位、格点、层次、原值和 manifest 的 failures。
首批确认后，把 request 换成同目录的 `request.json`，输出到新的 batch2 目录，处理全部8300个请求。
全量清单另含752个未构建案例，逐例记录本次查到的原因，不照抄旧日志的总计。

## 数据用来做什么

- **CAMS 城市原生时序**：重算污染物指导、边界层与扩散条件，核对D4不完整、D5缺口。
  保留原时间步、单位、缺测、全部原生层次；不插值填零、不任取首层当近地浓度。
  3小时瞬时O3最大不能冒充日最大8小时均值。
- **城市小时实况（六污染物及O3_8h）**：核对输入和五天真值、有效小时数，复核491个隔离案例。
  日值数据库可辅助核对，不能替代小时资料；不降低有效性阈值来恢复案例。
- **GFS/IFS及已有区域模式**：查变量、层次、发布时间记录；补齐缺失案例对应的城市多层时序和证据。
  特别核对温度廓线、边界层、风、湿度、垂直运动。只有少数气压层时明确其逆温识别限制。
- **JJJ_ATMO**：整理真实会商文本、蒸馏文件及出处/发布日期，供后续建立可追溯的方法卡。
  无出处或日期不明要明确记录，不能当成已核验专家方法。

后两项需本机另外整理，当前提取脚本只处理 CAMS 和城市观测。
有下载/发布 sidecar 一并保留；模式起报时刻和文件修改时间不能代替实际发布时间。

## 交回来什么

代码改动通过 Git 提交/推送。数据只回传压缩城市时序、必要证据/文本和审计清单，
保留原文件SHA256、使用的脚本版本及未解决缺口。
训练机接收目录：`/home/david/projects/wenjie/sitian/data/incoming/raw_handoff_20260912/`。
原始 NetCDF/GRIB 和完整观测档案留在9800X3D；不用上传 Git，不另做任务压缩包。

训练机收到结果后统一重建 case、历史/偏差索引、Parquet，核验统计口径、发布时间和切分隔离，
通过训练准入后再安排新训练。不要用旧 checkpoint 直接续跑。

## 当前代码状态

- Harness v2.1 已有质量查询、天气/污染相似案例检索、已验证历史真值查询和方法检索。
  方法检索目前只有6张标明来源的常识卡，尚无真实JJJ_ATMO卡。
- 已修复实际小时窗口、可见资料时间门禁、历史偏差的O3口径和重复记录问题；
  新数据行、工具配置、运行进程会核对资源身份，国域训练数据要求六污染物有效真值。
- 本轮测试281通过、2跳过。现有5658例输入工具检查通过；原生veRL六项工具与直接调用一致，提交流程通过。
  这不代表训练就绪：所有被检查案例仍有指导日期缺口、O3口径差异和发布时间待核验，1524例近期观测不完整。
- 详细实现及检查范围见 [Harness说明](docs/HARNESS_CAPABILITIES_2026-09-12.md)。
